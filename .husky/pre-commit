#!/bin/sh
echo "🛠️ Running app build before push..."

# Run your build step
if ! npm run build; then
  echo "❌ App build failed. Push aborted."
  exit 1
fi

echo "✅ App build successful. Proceeding with push."