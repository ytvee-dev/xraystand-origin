import {type ReactElement} from "react";
import Header from "@components/Header";
import DefaultDarkFooter from "@components/Footers/DefaultDarkFooter";
import "./style.css";
import {trafficLawsFooterMeta} from "@layout/Default/meta.ts";

export interface IDefaultLayoutProps {
    children: ReactElement;
}

const DefaultLayout = ({ children }: IDefaultLayoutProps): ReactElement => {
    return (
        <div className="default-layout" style={{ scrollbarWidth: "thin" }}>
            <Header/>
            <main>{ children }</main>
            <DefaultDarkFooter meta={trafficLawsFooterMeta}/>
        </div>
    );
};

export default DefaultLayout;
