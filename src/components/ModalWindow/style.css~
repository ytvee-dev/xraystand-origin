.modal-window-wrapper {
    position: fixed;
    top: 0;
    right: 0;

    width: 100%;
    height: 100%;

    z-index: 100;
    overflow: hidden;

    backdrop-filter: var(--modal-window-blur, blur(3px));

    display: flex;
    justify-content: center;
    align-items: center;
}

.modal-window-wrapper .modal {
    background-color: white;
}