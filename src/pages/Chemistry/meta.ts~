import type {TextContent} from "@pages/Chemistry/types.ts";

export const textContent: TextContent = {
    title: "Периодическая система химических\nэлементов Менделеева",
    description: "Элементы расположены по возрастанию атомного номера. Каждая группа — это элементы с похожими свойствами.",
};
